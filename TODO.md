- [x] Inspect existing Express app setup for CORS usage
- [x] Add and configure `cors` middleware in `src/app.js` (and ensure it runs before routes)
- [ ] Optionally add env vars for allowed origins/methods if present
- [ ] Restart backend and verify preflight (OPTIONS) handling from frontend
- [ ] Quick manual test: call a protected endpoint from frontend and confirm responses


